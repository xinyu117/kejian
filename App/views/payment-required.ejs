<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - 教育课件平台</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/public/css/style.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-graduation-cap me-2"></i>教育课件平台
            </a>
            <div class="navbar-nav ms-auto">
                <% if (typeof user !== 'undefined' && user) { %>
                    <div class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user me-1"></i><%= user.username %>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="#" onclick="logout()">
                                <i class="fas fa-sign-out-alt me-2"></i>退出登录
                            </a></li>
                        </ul>
                    </div>
                <% } %>
            </div>
        </div>
    </nav>

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-lg">
                <div class="card-body text-center p-5">
                    <div class="mb-4">
                        <i class="fas fa-lock text-warning" style="font-size: 4rem;"></i>
                    </div>
                    
                    <h2 class="mb-3">该课件需要VIP权限</h2>
                    <p class="text-muted mb-4">您正在访问的课件 "<%= courseware.title %>" 是付费内容，需要升级到VIP用户才能观看。</p>
                    
                    <div class="course-info bg-light p-4 rounded mb-4">
                        <h5><%= courseware.title %></h5>
                        <p class="text-muted mb-2"><%= courseware.description %></p>
                        <div class="d-flex justify-content-between align-items-center">
                            <span class="badge bg-primary"><%= courseware.category %></span>
                            <span class="fw-bold text-warning">¥<%= courseware.price %></span>
                        </div>
                    </div>

                    <div class="vip-benefits mb-4">
                        <h5>升级VIP享受以下权益：</h5>
                        <ul class="list-unstyled text-start">
                            <li class="mb-2">
                                <i class="fas fa-check text-success me-2"></i>
                                解锁全部付费课件
                            </li>
                            <li class="mb-2">
                                <i class="fas fa-check text-success me-2"></i>
                                无限次观看学习
                            </li>
                            <li class="mb-2">
                                <i class="fas fa-check text-success me-2"></i>
                                优先获得新课程更新
                            </li>
                            <li class="mb-2">
                                <i class="fas fa-check text-success me-2"></i>
                                专属客服支持
                            </li>
                        </ul>
                    </div>

                    <div class="pricing-card bg-gradient text-white p-4 rounded mb-4" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
                        <h4 class="mb-2">VIP会员</h4>
                        <div class="price-display">
                            <span class="h1">¥99</span>
                            <small>/永久</small>
                        </div>
                        <p class="mb-0">一次付费，终身享受</p>
                    </div>

                    <div class="d-grid gap-2">
                        <button class="btn btn-warning btn-lg" onclick="upgradeToVip()">
                            <i class="fas fa-crown me-2"></i>立即升级VIP
                        </button>
                        <a href="/" class="btn btn-outline-secondary">
                            <i class="fas fa-arrow-left me-2"></i>返回首页
                        </a>
                    </div>

                    <hr class="my-4">
                    
                    <div class="test-section">
                        <p class="small text-muted">测试环境快捷操作：</p>
                        <button class="btn btn-success btn-sm" onclick="simulateVipUpgrade()">
                            <i class="fas fa-magic me-1"></i>模拟VIP升级
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- VIP升级模态框 -->
<div class="modal fade" id="vipUpgradeModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="fas fa-crown text-warning me-2"></i>升级VIP会员
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body text-center">
                <div class="mb-4">
                    <div class="price-display">
                        <span class="h2 text-primary">¥99</span>
                        <small class="text-muted">/永久会员</small>
                    </div>
                </div>
                
                <div id="qrCodeSection" style="display: none;">
                    <p>请使用微信扫码支付：</p>
                    <img id="vipPaymentQR" src="" alt="VIP升级支付二维码" class="img-fluid mb-3" style="max-width: 200px;">
                    <p class="small text-muted">支付完成后将自动升级为VIP用户</p>
                </div>
                
                <div class="d-grid gap-2">
                    <button class="btn btn-primary" onclick="generateVipPayment()">
                        <i class="fas fa-qrcode me-2"></i>生成支付二维码
                    </button>
                    <hr>
                    <p class="small text-muted">测试环境：</p>
                    <button class="btn btn-success btn-sm" onclick="simulateVipPayment()">
                        <i class="fas fa-magic me-1"></i>模拟支付成功
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
function upgradeToVip() {
    const modal = new bootstrap.Modal(document.getElementById('vipUpgradeModal'));
    modal.show();
}

function generateVipPayment() {
    fetch('/api/payment/create', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({ amount: 99 })
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            document.getElementById('vipPaymentQR').src = data.qrCode;
            document.getElementById('qrCodeSection').style.display = 'block';
        } else {
            alert('生成支付二维码失败');
        }
    })
    .catch(error => {
        console.error('生成支付失败:', error);
    });
}

function simulateVipPayment() {
    fetch('/api/payment/simulate-success', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({})
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            alert('恭喜！VIP升级成功，即将刷新页面...');
            setTimeout(() => {
                window.location.reload();
            }, 1000);
        } else {
            alert('升级失败: ' + data.message);
        }
    })
    .catch(error => {
        console.error('模拟支付失败:', error);
    });
}

function simulateVipUpgrade() {
    simulateVipPayment();
}

function logout() {
    fetch('/api/auth/logout', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({})
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            window.location.href = '/login';
        }
    })
    .catch(error => {
        console.error('退出登录失败:', error);
    });
}
</script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 